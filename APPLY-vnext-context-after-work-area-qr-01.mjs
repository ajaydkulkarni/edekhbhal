import fs from "node:fs";
import { execSync } from "node:child_process";

const branch = execSync("git branch --show-current", { encoding: "utf8" }).trim();
if (branch !== "vNext") throw new Error(`Expected vNext branch, got ${branch}`);

const file = "docs/VNEXT-PROJECT-CONTEXT.md";
if (!fs.existsSync(file)) throw new Error(`${file} is missing.`);
let s = fs.readFileSync(file, "utf8");

function replaceExact(before, after, label) {
  if (!s.includes(before)) throw new Error(`Could not find expected ${label}; refusing partial context update.`);
  s = s.replace(before, after);
}

replaceExact("**Last updated:** 2026-09-02  \n**Primary rebuild branch:** `vNext`  \n**Latest validated vNext functional commit:** `b99b38f` \u2014 `Add vNext authentication and guided onboarding foundation`  \n**Previous validated database baseline:** `0109eb5` \u2014 `Establish vNext RLS database foundation`", "**Last updated:** 2026-09-03  \n**Primary rebuild branch:** `vNext`  \n**Latest validated vNext functional commit:** `3c360af` \u2014 `Add Work Area and QR lifecycle foundation`  \n**Previous validated functional baseline:** `b99b38f` \u2014 `Add vNext authentication and guided onboarding foundation`  \n**Validated database foundation:** `0109eb5` \u2014 `Establish vNext RLS database foundation`", "header baseline");

replaceExact("Onboarding creates only the **first Site**. Normal multi-Site administration belongs to the later Site management module.\n\n---\n\n## 9. Billing & Entitlements Foundation", "Onboarding creates only the **first Site**. Normal multi-Site administration belongs to the later Site management module.\n\n---\n\n## 9. Work Area + QR Lifecycle Foundation\n\nValidated functional commit:\n\n`3c360af Add Work Area and QR lifecycle foundation`\n\nImplemented hierarchy:\n\n`Organization \u2192 Site \u2192 Work Area`\n\nWork Area foundation includes:\n\n- explicit `organization_id` and parent `site_id`\n- name/code/description/location fields\n- ACTIVE/INACTIVE soft lifecycle\n- optimistic `version`\n- Site-scoped authorization\n- RLS + FORCE RLS\n- management writes limited to ADMIN and assigned SITE_MANAGER scope\n- `site_membership_scope` foundation for assignment-based Site access\n\nQR lifecycle includes:\n\n- one ACTIVE database-backed QR identity per Work Area\n- partial unique database constraint enforcing one active QR\n- random 48-character public token separate from internal UUIDs\n- Reprint preserves the same active QR identity\n- Regenerate atomically revokes the old QR and creates a new identity\n- old public QR becomes invalid after regeneration\n- create/regenerate idempotency records\n- row locking/concurrency protection\n- safe public resolver at `/q/[token]`\n- printable 4\u00d76 Work Area QR label\n- QR never grants application authorization\n\nThe public resolver intentionally exposes only safe transparency fields:\n\n- Organization name\n- Site name\n- Work Area name\n- Work Area description/location\n- service status\n\nIt does not expose worker contact details, private notes, audit history, memberships, or private evidence.\n\nThe initial QR token implementation used `gen_random_bytes`; the deployed database function search path did not expose that function. Migration `0005_work_area_qr_token_hotfix.sql` replaced token generation with two PostgreSQL UUIDv4 values truncated to 48 lowercase hex characters. The base `0004` migration was also repaired so clean replays do not reintroduce the defect.\n\nDemo parity includes synthetic Work Area + QR lifecycle behavior and explicitly explains Reprint, Regenerate, public scanning, and the rule that QR identity is not authorization.\n\n---\n\n## 10. Billing & Entitlements Foundation", "Work Area section insertion");

for (let n = 21; n >= 10; n--) {
  s = s.replace(`## ${n}. `, `## ${n + 1}. `);
}

replaceExact("Authentication/onboarding foundation currently audits:\n\n- `ORGANIZATION_CREATED`\n- `FREE_PLAN_ACTIVATED`\n- `SITE_CREATED`\n\nAll future meaningful changes require change-of-value testing where applicable.", "Current validated audit coverage includes:\n\n- `ORGANIZATION_CREATED`\n- `FREE_PLAN_ACTIVATED`\n- `SITE_CREATED`\n- `WORK_AREA_CREATED`\n- `QR_ISSUED`\n- `QR_REGENERATED`\n- `WORK_AREA_STATUS_CHANGED`\n\nAll future meaningful changes require change-of-value testing where applicable.", "audit coverage");

replaceExact("Current Demo onboarding illustrates:\n\n- User details\n- Organization creation\n- plan selection\n- first Site\n- workspace handoff\n\nIt explicitly explains that real authentication, billing writes, evidence capture, and destructive actions are unavailable in Demo.", "Current Demo illustrates:\n\n- User details\n- Organization creation\n- plan selection\n- first Site\n- workspace handoff\n- synthetic Work Area lifecycle\n- QR Reprint vs Regenerate behavior\n- safe public QR transparency\n- the rule that QR identity never grants authorization\n\nIt explicitly explains that real authentication, tenant writes, QR regeneration, billing writes, evidence capture, and destructive actions are unavailable in Demo.", "Demo coverage");

const baselineStart = s.indexOf("## 19. Current Validated Test Baseline");
const baselineEnd = s.indexOf("---\n\n## 20. Current Technology Baseline");
if (baselineStart < 0 || baselineEnd < 0 || baselineEnd <= baselineStart) throw new Error("Could not locate validated test baseline section.");
s = s.slice(0, baselineStart) + "## 19. Current Validated Test Baseline\n\nAt commit `3c360af`, the validated development run was:\n\n### Integration\n\n- Database Foundation RLS tests: **13/13 PASS**\n- Auth + Onboarding integration tests: **6/6 PASS**\n- Work Area + QR integration tests: **7/7 PASS**\n- Total integration: **26/26 PASS**\n\nWork Area + QR integration covers:\n\n- isolated tenant fixture\n- idempotent Work Area creation\n- exactly one active QR\n- Reprint identity preservation\n- atomic/idempotent QR regeneration\n- old QR invalidation and new QR public resolution\n- optimistic Work Area status versioning\n- fail-closed tenant reads\n- safe public-field boundary\n- creation/QR/status audit evidence\n\n### Full Vitest\n\n- Test files: **8/8 PASS**\n- Tests: **37/37 PASS**\n\nIncludes:\n\n- RLS integration\n- onboarding DB boundary integration\n- Work Area + QR lifecycle integration\n- tenant context unit tests\n- audit diff unit tests\n- onboarding state-machine tests\n- foundation contract tests\n- onboarding route-state module tests\n\n### Build gates\n\n- TypeScript: **PASS**\n- ESLint: **PASS**\n- Next.js production build: **PASS**\n\nBuild routes include:\n\n- `/`\n- `/demo`\n- `/login`\n- `/register`\n- `/auth/callback`\n- `/onboarding/profile`\n- `/onboarding/organization`\n- `/onboarding/plan`\n- `/onboarding/site`\n- `/workspace`\n- `/workspace/work-areas`\n- `/workspace/work-areas/[id]/qr`\n- `/q/[token]`\n\nNext.js Proxy middleware is active.\n\n### Playwright\n\n- Public Auth + Demo onboarding paths: PASS\n- Landing primary product paths: PASS\n- Demo Work Area + QR lifecycle: PASS\n- Total: **3/3 PASS**\n\nThe prior Next.js `127.0.0.1` development HMR warning remains addressed through `allowedDevOrigins`.\n\n" + s.slice(baselineEnd);

const nextStart = s.indexOf("## 21. Immediate Next Development Target");
const nextEnd = s.indexOf("---\n\n## 22. Important Deferred Items");
if (nextStart < 0 || nextEnd < 0 || nextEnd <= nextStart) throw new Error("Could not locate immediate next target section.");
s = s.slice(0, nextStart) + "## 21. Immediate Next Development Target\n\nNext functional increment:\n\n**Task Master Foundation 01**\n\nThe increment should establish:\n\n1. Organization-level reusable Task master\n2. name and rich task instructions/description\n3. ACTIVE/INACTIVE lifecycle\n4. authorization: ADMIN and permitted SITE_MANAGER management, USER read-only\n5. attachment metadata contract prepared for private object storage\n6. no Base64 media storage\n7. audit old/new values for create/edit/status changes\n8. RLS + FORCE RLS\n9. optimistic versioning\n10. idempotent create/update command boundaries where applicable\n11. Demo parity with representative synthetic Tasks\n12. deterministic seed/test data\n13. unit/module/integration/RLS/change-of-value coverage\n14. workspace Task administration UI\n15. API/service contracts suitable for later Schedule composition\n\nDo not implement Schedule/Occurrence execution in the Task increment. Task must remain a reusable Organization-level master and must not be coupled to a specific Site or Work Area.\n\n" + s.slice(nextEnd);

replaceExact("- multi-Site administration UI\n- Work Areas\n- QR lifecycle\n- Tasks", "- multi-Site administration UI\n- Tasks", "deferred items");

replaceExact("Key validated vNext commits:\n\n- `0109eb5` \u2014 Establish vNext RLS database foundation\n- `b99b38f` \u2014 Add vNext authentication and guided onboarding foundation\n\n`b99b38f` is the current functional baseline for subsequent vNext development.", "Key validated vNext commits:\n\n- `0109eb5` \u2014 Establish vNext RLS database foundation\n- `b99b38f` \u2014 Add vNext authentication and guided onboarding foundation\n- `3c360af` \u2014 Add Work Area and QR lifecycle foundation\n\n`3c360af` is the current locked functional baseline for subsequent vNext development.", "baseline commit chain");

fs.writeFileSync(file, s, "utf8");
console.log("Updated docs/VNEXT-PROJECT-CONTEXT.md for validated commit 3c360af.");
