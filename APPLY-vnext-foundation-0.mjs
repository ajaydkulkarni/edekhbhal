import fs from "node:fs";
import path from "node:path";
import { execSync } from "node:child_process";

const root = process.cwd();
const branch = execSync("git branch --show-current", { encoding: "utf8" }).trim();
if (branch !== "vNext") throw new Error(`Refusing to run: expected vNext, got ${branch}`);
const status = execSync("git status --porcelain", { encoding: "utf8" }).trim();
const allowed = new Set(["?? APPLY-vnext-foundation-0.mjs"]);
if (status && !status.split("\n").every((line) => allowed.has(line))) {
  throw new Error("Refusing to run: working tree is not clean.");
}

const files = new Map();
files.set(".env.example", "# Public Supabase browser configuration\nNEXT_PUBLIC_SUPABASE_URL=\nNEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=\n\n# Server-only pooled PostgreSQL URL for the runtime role.\n# Do not put this in NEXT_PUBLIC_*.\nDATABASE_URL=\n\n# Direct database URL used only for migrations/admin tooling.\n# Keep separate from the runtime connection.\nMIGRATION_DATABASE_URL=\n\n# Product-neutral branding\nNEXT_PUBLIC_PRODUCT_NAME=Operations Platform\nNEXT_PUBLIC_SUPPORT_EMAIL=support@example.com\n\n# Tests\nTEST_DATABASE_URL=\n");
files.set(".github/workflows/ci.yml", "name: vNext CI\n\non:\n  push:\n    branches: [vNext]\n  pull_request:\n    branches: [vNext]\n\npermissions:\n  contents: read\n\njobs:\n  quality:\n    runs-on: ubuntu-latest\n    timeout-minutes: 15\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: 22\n          cache: npm\n      - run: npm ci\n      - run: npm run lint\n      - run: npm run typecheck\n      - run: npm run test\n      - run: npm run build\n\n  e2e:\n    runs-on: ubuntu-latest\n    timeout-minutes: 15\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: 22\n          cache: npm\n      - run: npm ci\n      - run: npx playwright install --with-deps chromium\n      - run: npm run test:e2e\n");
files.set(".gitignore", ".next/\nnode_modules/\nout/\ndist/\ncoverage/\nplaywright-report/\ntest-results/\n.env\n.env.local\n.env.*.local\n!.env.example\n.DS_Store\n*.log\n*.tsbuildinfo\n");
files.set("README.md", "# vNext\n\nClean-slate rebuild of the operations platform.\n\n## Architectural baseline\n\n- Product-name neutral\n- Next.js + TypeScript\n- Drizzle + PostgreSQL/Supabase\n- Supabase Auth\n- RLS from the first tenant table\n- Uniform append-only Audit\n- Explicit Organization context per request/transaction\n- Multi-Organization User support\n- UTC execution timestamps + IANA scheduling intent\n- Demo parity\n- Provider-neutral Billing/Entitlements\n- Stripe first; Razorpay later\n- Unit, module, integration, RLS, change-of-value, concurrency, E2E and load testing\n\nSee `docs/VNEXT-ARCHITECTURE-BASELINE.md`.\n");
files.set("docs/FOUNDATION-0-INSTRUCTIONS.md", "# Foundation 0 \u2014 Exact Installation Instructions\n\n## Preconditions\n\nYou must already be on the clean `vNext` branch and `git status` must be clean.\n\nDo not delete the existing Supabase project or Vercel deployment.\n\n## Install this package\n\nAfter extracting/copying the Foundation 0 files into `/workspaces/edekhbhal`:\n\n```bash\ncd /workspaces/edekhbhal\n\ngit branch --show-current\ngit status\n\nnpm install\n\nnpm run lint\nnpm run typecheck\nnpm run test\nnpm run build\n```\n\nThen install Playwright Chromium and run the landing-page E2E test:\n\n```bash\nnpx playwright install chromium\nnpm run test:e2e\n```\n\nIf all commands pass:\n\n```bash\ngit add .\ngit commit -m \"vNext Foundation 0 application skeleton\"\ngit push origin vNext\n```\n\nThen send the full terminal output back for verification.\n\n## Do not configure Supabase yet\n\nThe Supabase clients and Drizzle foundation are present, but the landing page deliberately does not require environment variables.\n\nAfter this package is validated and committed, the next guided step is:\n1. create the new Supabase project;\n2. collect the new project URL/publishable key and database URLs;\n3. create dedicated database roles;\n4. apply the first RLS-native migrations;\n5. enable the database/RLS integration test suite.\n\nDo not reuse the legacy Supabase connection strings.\n");
files.set("docs/VNEXT-ARCHITECTURE-BASELINE.md", "# vNext Architecture Baseline\n\nThis repository is a clean-slate rebuild. The legacy implementation is a behavioral reference only.\n\n## Locked decisions\n\n- Canonical hierarchy: Organization \u2192 Site \u2192 Work Area.\n- Customer roles: ADMIN, SITE_MANAGER, USER.\n- One global User may belong to multiple Organizations with different roles.\n- Tenant Organization is explicit per request/transaction.\n- PostgreSQL RLS is a database invariant.\n- Execution timestamps are server-authoritative UTC.\n- Local schedule intent preserves IANA timezone semantics.\n- Open work is default; specific Occurrence assignment is optional.\n- My Work and Work Area QR are equal entry paths into eligible execution.\n- Previous/Next navigates tasks without changing task state.\n- Audit format: Date/Time | User | IP Address | Module | Action | Old Value | New Value.\n- Demo Workspace is synthetic, not a shared tenant.\n- Registration path: User details \u2192 Organization \u2192 Plan \u2192 payment/free activation \u2192 first Site \u2192 workspace.\n- Stripe activates first; Razorpay/India follows behind the same billing gateway.\n- No legacy data migration; deterministic seed data will be created.\n- The legacy Supabase and Vercel deployments remain untouched until vNext validation/cutover.\n");
files.set("drizzle.config.ts", "import { defineConfig } from \"drizzle-kit\";\n\nconst url = process.env.MIGRATION_DATABASE_URL ?? process.env.DATABASE_URL;\n\nif (!url) {\n  throw new Error(\"MIGRATION_DATABASE_URL or DATABASE_URL is required for Drizzle commands.\");\n}\n\nexport default defineConfig({\n  schema: \"./src/db/schema.ts\",\n  out: \"./drizzle\",\n  dialect: \"postgresql\",\n  dbCredentials: { url },\n  strict: true,\n  verbose: true,\n});\n");
files.set("e2e/landing.spec.ts", "import { expect, test } from \"@playwright/test\";\n\ntest(\"landing page exposes the primary product paths\", async ({ page }) => {\n  await page.goto(\"/\");\n  await expect(page.getByRole(\"heading\", { level: 1 })).toContainText(\"Every task\");\n  await expect(page.getByRole(\"link\", { name: \"Start free\" }).first()).toBeVisible();\n  await expect(page.getByRole(\"link\", { name: \"View Demo\" })).toBeVisible();\n  await expect(page.getByRole(\"link\", { name: \"Sign in\" })).toBeVisible();\n});\n");
files.set("eslint.config.mjs", "import { defineConfig, globalIgnores } from \"eslint/config\";\nimport nextVitals from \"eslint-config-next/core-web-vitals\";\n\nexport default defineConfig([\n  ...nextVitals,\n  globalIgnores([\n    \".next/**\",\n    \"node_modules/**\",\n    \"coverage/**\",\n    \"playwright-report/**\",\n    \"test-results/**\",\n    \"drizzle/**\",\n  ]),\n]);\n");
files.set("next-env.d.ts", "/// <reference types=\"next\" />\n/// <reference types=\"next/image-types/global\" />\n\n// NOTE: This file should not be edited manually.\n");
files.set("next.config.ts", "import type { NextConfig } from \"next\";\n\nconst nextConfig: NextConfig = {\n  poweredByHeader: false,\n  reactStrictMode: true,\n  experimental: {\n    typedEnv: true,\n  },\n  async headers() {\n    return [\n      {\n        source: \"/(.*)\",\n        headers: [\n          { key: \"X-Content-Type-Options\", value: \"nosniff\" },\n          { key: \"Referrer-Policy\", value: \"strict-origin-when-cross-origin\" },\n          { key: \"X-Frame-Options\", value: \"DENY\" },\n          { key: \"Permissions-Policy\", value: \"camera=(), microphone=(), geolocation=()\" },\n        ],\n      },\n    ];\n  },\n};\n\nexport default nextConfig;\n");
files.set("package.json", "{\n  \"name\": \"operations-platform-vnext\",\n  \"version\": \"0.1.0\",\n  \"private\": true,\n  \"engines\": {\n    \"node\": \">=22.0.0\"\n  },\n  \"scripts\": {\n    \"dev\": \"next dev\",\n    \"build\": \"next build\",\n    \"start\": \"next start\",\n    \"lint\": \"eslint .\",\n    \"typecheck\": \"tsc --noEmit\",\n    \"test\": \"vitest run\",\n    \"test:unit\": \"vitest run tests/unit\",\n    \"test:module\": \"vitest run tests/module\",\n    \"test:integration\": \"vitest run tests/integration\",\n    \"test:watch\": \"vitest\",\n    \"test:e2e\": \"playwright test\",\n    \"test:foundation\": \"npm run lint && npm run typecheck && npm run test && npm run build\",\n    \"db:generate\": \"drizzle-kit generate\",\n    \"db:check\": \"drizzle-kit check\"\n  },\n  \"dependencies\": {\n    \"@supabase/ssr\": \"0.12.5\",\n    \"@supabase/supabase-js\": \"2.112.4\",\n    \"drizzle-orm\": \"0.45.2\",\n    \"next\": \"16.3.4\",\n    \"postgres\": \"3.4.7\",\n    \"react\": \"19.2.8\",\n    \"react-dom\": \"19.2.8\",\n    \"zod\": \"4.5.4\"\n  },\n  \"devDependencies\": {\n    \"@playwright/test\": \"1.62.1\",\n    \"@types/node\": \"26.4.0\",\n    \"@types/react\": \"19.2.18\",\n    \"@types/react-dom\": \"19.2.5\",\n    \"drizzle-kit\": \"0.31.10\",\n    \"eslint\": \"10.9.1\",\n    \"eslint-config-next\": \"16.3.4\",\n    \"typescript\": \"7.0.2\",\n    \"vitest\": \"4.1.11\"\n  }\n}\n");
files.set("playwright.config.ts", "import { defineConfig, devices } from \"@playwright/test\";\n\nexport default defineConfig({\n  testDir: \"./e2e\",\n  fullyParallel: true,\n  forbidOnly: !!process.env.CI,\n  retries: process.env.CI ? 2 : 0,\n  workers: process.env.CI ? 2 : undefined,\n  reporter: [[\"html\", { open: \"never\" }], [\"list\"]],\n  use: {\n    baseURL: process.env.PLAYWRIGHT_BASE_URL ?? \"http://127.0.0.1:3000\",\n    trace: \"on-first-retry\",\n    screenshot: \"only-on-failure\",\n  },\n  projects: [\n    { name: \"chromium\", use: { ...devices[\"Desktop Chrome\"] } },\n  ],\n  webServer: process.env.PLAYWRIGHT_BASE_URL\n    ? undefined\n    : {\n        command: \"npm run dev\",\n        url: \"http://127.0.0.1:3000\",\n        reuseExistingServer: !process.env.CI,\n        timeout: 120_000,\n      },\n});\n");
files.set("src/app/demo/page.tsx", "import Link from \"next/link\";\nimport { brand } from \"@/lib/brand\";\n\nexport default function DemoPage() {\n  return (\n    <main className=\"simplePage\">\n      <div className=\"simpleCard\">\n        <span className=\"eyebrow\">BEST PRACTICE DEMO</span>\n        <h1>Explore {brand.productName}</h1>\n        <p>\n          The universal Demo Workspace will be implemented alongside every real feature.\n          It will use synthetic data and never become a shared tenant Organization.\n        </p>\n        <Link className=\"button\" href=\"/\">Back to landing page</Link>\n      </div>\n    </main>\n  );\n}\n");
files.set("src/app/globals.css", "*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:#f7f9fc;color:#10233f;font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif}.shell{width:min(1180px,calc(100% - 40px));margin:0 auto}.nav{height:82px;display:flex;align-items:center;justify-content:space-between;gap:28px}.brand{display:flex;align-items:center;gap:10px;text-decoration:none;color:#10233f;font-weight:800;letter-spacing:-.02em}.brandMark{width:34px;height:34px;border-radius:11px;display:grid;place-items:center;color:white;background:linear-gradient(145deg,#1163e6,#05a6b8);box-shadow:0 8px 22px #1163e633}.navLinks{display:flex;gap:26px}.navLinks a,.textButton{color:#50627b;text-decoration:none;font-size:14px;font-weight:650}.navActions{display:flex;align-items:center;gap:14px}.button{display:inline-flex;align-items:center;justify-content:center;border:0;border-radius:13px;padding:14px 21px;background:#1163e6;color:white;text-decoration:none;font-weight:750;box-shadow:0 12px 24px #1163e628;transition:.2s transform,.2s box-shadow}.button:hover{transform:translateY(-2px);box-shadow:0 16px 30px #1163e638}.button.small{padding:10px 16px;border-radius:11px}.button.secondary{background:white;color:#173353;border:1px solid #dce5f0;box-shadow:none}.button.light{background:white;color:#12345a}.hero{min-height:680px;display:grid;grid-template-columns:1.03fr .97fr;align-items:center;gap:70px;padding-top:36px;padding-bottom:78px}.eyebrow{font-size:12px;letter-spacing:.16em;font-weight:850;color:#0c75b9;margin-bottom:16px}.hero h1{font-size:clamp(52px,6.3vw,82px);line-height:.98;letter-spacing:-.055em;margin:0;max-width:700px}.hero h1 span{background:linear-gradient(100deg,#1163e6,#079a9c);-webkit-background-clip:text;color:transparent}.heroLead{font-size:19px;line-height:1.65;color:#5a6c83;max-width:640px;margin:28px 0}.heroActions{display:flex;gap:12px}.trustLine{display:flex;gap:24px;flex-wrap:wrap;margin-top:26px;color:#718096;font-size:13px}.heroVisual{height:520px;position:relative;display:grid;place-items:center}.phone{width:255px;height:500px;background:#0c1c31;border:8px solid #0c1c31;border-radius:38px;box-shadow:0 40px 75px #183b6830;position:relative;z-index:3;transform:rotate(2deg)}.phoneTop{width:86px;height:20px;background:#0c1c31;border-radius:0 0 14px 14px;position:absolute;top:-1px;left:50%;transform:translateX(-50%);z-index:2}.phoneContent{background:linear-gradient(180deg,#f9fbff,#edf4fb);height:100%;border-radius:29px;padding:50px 18px 20px}.miniLabel{font-size:10px;font-weight:900;letter-spacing:.13em;color:#4980a5}.phoneContent h3{margin:15px 0 4px;font-size:22px}.phoneContent p{color:#6c7e91;margin:0 0 14px}.status{background:#e6f7ef;color:#147a50;padding:8px 11px;border-radius:10px;font-size:12px;font-weight:800}.qr{width:135px;height:135px;margin:25px auto 20px;background:white;border:1px solid #dbe5ef;border-radius:20px;display:grid;place-items:center;box-shadow:0 12px 24px #183b6812}.qrGrid{font-size:88px;line-height:1;color:#132b45}.phone button{width:100%;border:0;padding:12px;border-radius:11px;background:#1163e6;color:#fff;font-weight:800}.floating{position:absolute;background:#fff;border:1px solid #e2eaf3;border-radius:18px;padding:17px 18px;box-shadow:0 26px 55px #17395f1f;z-index:4}.cardA{width:240px;left:0;top:75px}.cardB{width:230px;right:-10px;bottom:70px}.floating strong{display:block;margin:7px 0;font-size:17px}.floating small{display:block;color:#718096;line-height:1.5}.muted{margin-top:4px;font-size:10px}.bar{height:7px;background:#e9eef5;border-radius:9px;overflow:hidden;margin:11px 0}.bar i{height:100%;display:block;background:linear-gradient(90deg,#16a36b,#00a2a7);border-radius:9px}.orbit{position:absolute;border-radius:999px;border:1px solid #cfe0f3}.orbit1{width:430px;height:430px}.orbit2{width:540px;height:540px;border-style:dashed;opacity:.55}.proofStrip{border-top:1px solid #e1e8f0;border-bottom:1px solid #e1e8f0;background:white}.proofInner{min-height:88px;display:flex;align-items:center;justify-content:space-between;gap:28px;flex-wrap:wrap;color:#738399;font-size:13px}.proofInner strong{color:#29445f}.section{padding:110px 0}.sectionHeading{max-width:740px;margin-bottom:48px}.section h2{font-size:clamp(36px,4vw,54px);line-height:1.08;letter-spacing:-.045em;margin:0 0 18px}.section p{color:#63758b;line-height:1.7;font-size:17px}.steps{display:grid;grid-template-columns:repeat(4,1fr);gap:18px}.step{background:white;border:1px solid #e0e8f1;padding:28px;border-radius:22px;box-shadow:0 14px 35px #1b385c0a}.step>span{display:grid;place-items:center;width:38px;height:38px;border-radius:12px;background:#eaf3ff;color:#1262cf;font-weight:900}.step h3{font-size:22px;margin:21px 0 8px}.step p{font-size:14px;margin:0}.tinted{width:100%;background:#edf4fb}.split{display:grid;grid-template-columns:.9fr 1.1fr;gap:80px;align-items:center}.securityGrid{display:grid;grid-template-columns:1fr 1fr;gap:15px}.securityGrid div{background:#fff;padding:24px;border:1px solid #dce7f2;border-radius:18px}.securityGrid strong,.securityGrid span{display:block}.securityGrid strong{margin-bottom:7px}.securityGrid span{font-size:13px;color:#6e8093;line-height:1.5}.cta{background:linear-gradient(125deg,#0d2f57,#0a6f86);color:white;border-radius:30px;padding:55px 60px;display:flex;align-items:center;justify-content:space-between;gap:50px;box-shadow:0 30px 65px #0f426533}.cta h2{font-size:42px;max-width:680px}.cta p{color:#c6deea;max-width:700px}.cta .eyebrow{color:#77d8e1}.footer{padding:35px 0 50px;border-top:1px solid #e0e8ef;display:flex;justify-content:space-between;color:#7a8a9c;font-size:12px}@media(max-width:920px){.navLinks{display:none}.hero{grid-template-columns:1fr;padding-top:60px}.heroVisual{margin-top:20px}.steps{grid-template-columns:1fr 1fr}.split{grid-template-columns:1fr}.cta{flex-direction:column;align-items:flex-start}.cardA{left:5%}.cardB{right:4%}}@media(max-width:620px){.shell{width:min(100% - 24px,1180px)}.nav{height:70px}.navActions .textButton:first-child{display:none}.hero{gap:30px;padding-bottom:50px}.hero h1{font-size:48px}.heroVisual{height:475px;transform:scale(.9)}.cardA{left:-2%}.cardB{right:-2%}.steps,.securityGrid{grid-template-columns:1fr}.section{padding:75px 0}.cta{padding:38px 26px}.footer{flex-direction:column;gap:8px}}.simplePage{min-height:100vh;display:grid;place-items:center;padding:30px;background:radial-gradient(circle at 20% 10%,#dcecff 0,transparent 36%),#f6f9fc}.simpleCard{width:min(620px,100%);background:white;border:1px solid #dfe8f1;border-radius:28px;padding:48px;box-shadow:0 30px 70px #183b6819}.simpleCard h1{font-size:46px;letter-spacing:-.045em;margin:0 0 16px}.simpleCard p{color:#66788e;line-height:1.7;margin-bottom:28px}");
files.set("src/app/layout.tsx", "import type { Metadata } from \"next\";\nimport \"./globals.css\";\nimport { brand } from \"@/lib/brand\";\n\nexport const metadata: Metadata = {\n  title: `${brand.productName} | Operational work, verified`,\n  description:\n    \"Plan recurring operational work, execute it where the work happens, capture evidence, and see results in real time.\",\n};\n\nexport default function RootLayout({\n  children,\n}: Readonly<{ children: React.ReactNode }>) {\n  return (\n    <html lang=\"en\">\n      <body>{children}</body>\n    </html>\n  );\n}\n");
files.set("src/app/login/page.tsx", "import Link from \"next/link\";\n\nexport default function LoginPage() {\n  return (\n    <main className=\"simplePage\">\n      <div className=\"simpleCard\">\n        <span className=\"eyebrow\">SIGN IN</span>\n        <h1>Welcome back.</h1>\n        <p>Supabase Auth will be connected after the new vNext Supabase project is created.</p>\n        <Link className=\"button\" href=\"/\">Back</Link>\n      </div>\n    </main>\n  );\n}\n");
files.set("src/app/page.tsx", "import Link from \"next/link\";\nimport { brand } from \"@/lib/brand\";\n\nconst industries = [\"Hospitality\", \"Manufacturing\", \"Healthcare\", \"Offices\", \"Maintenance\"];\n\nconst steps = [\n  [\"1\", \"Organize\", \"Structure operations as Organization \u2192 Site \u2192 Work Area.\"],\n  [\"2\", \"Plan\", \"Create reusable tasks and schedules with timing, evidence and document control.\"],\n  [\"3\", \"Execute\", \"Users follow My Work or scan a nearby Work Area QR to find eligible work.\"],\n  [\"4\", \"Verify\", \"Capture evidence, timing, notes and a consistent end-to-end audit trail.\"],\n];\n\nexport default function Home() {\n  return (\n    <main>\n      <header className=\"nav shell\">\n        <Link className=\"brand\" href=\"/\" aria-label=\"Home\">\n          <span className=\"brandMark\">O</span>\n          <span>{brand.productName}</span>\n        </Link>\n        <nav className=\"navLinks\" aria-label=\"Primary navigation\">\n          <a href=\"#how\">How it works</a>\n          <a href=\"#industries\">Industries</a>\n          <a href=\"#security\">Security</a>\n          <a href=\"#pricing\">Pricing</a>\n        </nav>\n        <div className=\"navActions\">\n          <Link className=\"textButton\" href=\"/demo\">View Demo</Link>\n          <Link className=\"textButton\" href=\"/login\">Sign in</Link>\n          <Link className=\"button small\" href=\"/register\">Start free</Link>\n        </div>\n      </header>\n\n      <section className=\"hero shell\">\n        <div className=\"heroCopy\">\n          <div className=\"eyebrow\">OPERATIONS, WITHOUT THE CHAOS</div>\n          <h1>Every task. Every Site. <span>Verified.</span></h1>\n          <p className=\"heroLead\">\n            Plan recurring work, let teams execute where they are, capture proof,\n            and see what is actually happening\u2014without forcing operations into a rigid route.\n          </p>\n          <div className=\"heroActions\">\n            <Link className=\"button\" href=\"/register\">Start free</Link>\n            <Link className=\"button secondary\" href=\"/demo\">Explore the demo</Link>\n          </div>\n          <div className=\"trustLine\">\n            <span>\u2713 Open or assigned work</span>\n            <span>\u2713 QR-first execution</span>\n            <span>\u2713 Complete auditability</span>\n          </div>\n        </div>\n\n        <div className=\"heroVisual\" aria-label=\"Illustration of live operations\">\n          <div className=\"floating card cardA\">\n            <div className=\"miniLabel\">LIVE OPERATIONS</div>\n            <strong>18 due now</strong>\n            <div className=\"bar\"><i style={{ width: \"72%\" }} /></div>\n            <small>12 complete \u00b7 4 in progress \u00b7 2 open</small>\n          </div>\n          <div className=\"phone\">\n            <div className=\"phoneTop\" />\n            <div className=\"phoneContent\">\n              <span className=\"miniLabel\">MY WORK</span>\n              <h3>Sanitation Checklist</h3>\n              <p>Packaging Line 2</p>\n              <div className=\"status\">Open \u00b7 Due 8:15 AM</div>\n              <div className=\"qr\">\n                <div className=\"qrGrid\">\u25a6</div>\n              </div>\n              <button type=\"button\">Claim &amp; Start</button>\n            </div>\n          </div>\n          <div className=\"floating card cardB\">\n            <div className=\"miniLabel\">AUDIT</div>\n            <strong>Reassigned</strong>\n            <small>Ravi Patel \u2192 Priya Shah</small>\n            <small className=\"muted\">User \u00b7 IP \u00b7 Old \u00b7 New</small>\n          </div>\n          <div className=\"orbit orbit1\" />\n          <div className=\"orbit orbit2\" />\n        </div>\n      </section>\n\n      <section className=\"proofStrip\" id=\"industries\">\n        <div className=\"shell proofInner\">\n          <span>Built for operational work across</span>\n          {industries.map((industry) => <strong key={industry}>{industry}</strong>)}\n        </div>\n      </section>\n\n      <section className=\"section shell\" id=\"how\">\n        <div className=\"sectionHeading\">\n          <div className=\"eyebrow\">ONE SIMPLE OPERATING MODEL</div>\n          <h2>From plan to proof, without losing the human flexibility.</h2>\n          <p>The next job can be recommended. It does not have to dictate the worker&apos;s physical route.</p>\n        </div>\n        <div className=\"steps\">\n          {steps.map(([n, title, copy]) => (\n            <article className=\"step\" key={n}>\n              <span>{n}</span>\n              <h3>{title}</h3>\n              <p>{copy}</p>\n            </article>\n          ))}\n        </div>\n      </section>\n\n      <section className=\"section tinted\" id=\"security\">\n        <div className=\"shell split\">\n          <div>\n            <div className=\"eyebrow\">DESIGNED FOR TRUST</div>\n            <h2>Security is part of the data model.</h2>\n            <p>\n              Tenant isolation, RLS, immutable execution history, explicit Organization context,\n              private evidence storage, and uniform audit trails are built into the architecture\u2014not added later.\n            </p>\n          </div>\n          <div className=\"securityGrid\">\n            <div><strong>RLS-native</strong><span>Organization isolation at PostgreSQL level</span></div>\n            <div><strong>Time-correct</strong><span>UTC execution + local scheduling intent</span></div>\n            <div><strong>Auditable</strong><span>Date/Time \u00b7 User \u00b7 IP \u00b7 Old \u00b7 New</span></div>\n            <div><strong>Concurrency-safe</strong><span>Claims and assignments protected in the database</span></div>\n          </div>\n        </div>\n      </section>\n\n      <section className=\"section shell\" id=\"pricing\">\n        <div className=\"cta\">\n          <div>\n            <div className=\"eyebrow\">START SIMPLE. GROW WHEN YOU NEED TO.</div>\n            <h2>Local pricing. Flexible plans. No operational lock-in.</h2>\n            <p>Free, sponsored, seat-based and feature-based plans are supported by the same entitlement architecture.</p>\n          </div>\n          <Link className=\"button light\" href=\"/register\">Create your workspace</Link>\n        </div>\n      </section>\n\n      <footer className=\"footer shell\">\n        <span>\u00a9 2026 {brand.productName}</span>\n        <span>{brand.tagline}</span>\n      </footer>\n    </main>\n  );\n}\n");
files.set("src/app/register/page.tsx", "import Link from \"next/link\";\n\nexport default function RegisterPage() {\n  return (\n    <main className=\"simplePage\">\n      <div className=\"simpleCard\">\n        <span className=\"eyebrow\">START FREE</span>\n        <h1>Create your account.</h1>\n        <p>\n          Guided onboarding will continue through Profile \u2192 Organization \u2192 Plan \u2192 First Site,\n          then release the user into the workspace.\n        </p>\n        <Link className=\"button\" href=\"/\">Back</Link>\n      </div>\n    </main>\n  );\n}\n");
files.set("src/db/client.ts", "import { drizzle } from \"drizzle-orm/postgres-js\";\nimport postgres from \"postgres\";\nimport * as schema from \"./schema\";\n\nlet sqlClient: ReturnType<typeof postgres> | undefined;\n\nexport function getDatabase() {\n  const url = process.env.DATABASE_URL;\n  if (!url) {\n    throw new Error(\"DATABASE_URL is not configured.\");\n  }\n\n  sqlClient ??= postgres(url, {\n    max: 5,\n    idle_timeout: 20,\n    connect_timeout: 10,\n    prepare: false,\n  });\n\n  return drizzle(sqlClient, { schema });\n}\n");
files.set("src/db/schema.ts", "import {\n  pgEnum,\n  pgTable,\n  uuid,\n  text,\n  timestamp,\n  bigint,\n  jsonb,\n  uniqueIndex,\n  index,\n} from \"drizzle-orm/pg-core\";\n\nexport const membershipRole = pgEnum(\"membership_role\", [\n  \"ADMIN\",\n  \"SITE_MANAGER\",\n  \"USER\",\n]);\n\nexport const recordStatus = pgEnum(\"record_status\", [\"ACTIVE\", \"INACTIVE\"]);\n\nexport const organizations = pgTable(\"organization\", {\n  id: uuid(\"id\").defaultRandom().primaryKey(),\n  name: text(\"name\").notNull(),\n  countryCode: text(\"country_code\").notNull(),\n  defaultCurrencyCode: text(\"default_currency_code\").notNull(),\n  defaultTimezone: text(\"default_timezone\").notNull(),\n  status: recordStatus(\"status\").default(\"ACTIVE\").notNull(),\n  createdAt: timestamp(\"created_at\", { withTimezone: true }).defaultNow().notNull(),\n  updatedAt: timestamp(\"updated_at\", { withTimezone: true }).defaultNow().notNull(),\n  version: bigint(\"version\", { mode: \"number\" }).default(1).notNull(),\n});\n\nexport const appUsers = pgTable(\"app_user\", {\n  id: uuid(\"id\").defaultRandom().primaryKey(),\n  authSubject: text(\"auth_subject\").notNull().unique(),\n  email: text(\"email\"),\n  displayName: text(\"display_name\"),\n  preferredLanguage: text(\"preferred_language\").default(\"en\").notNull(),\n  status: recordStatus(\"status\").default(\"ACTIVE\").notNull(),\n  createdAt: timestamp(\"created_at\", { withTimezone: true }).defaultNow().notNull(),\n  updatedAt: timestamp(\"updated_at\", { withTimezone: true }).defaultNow().notNull(),\n});\n\nexport const organizationMemberships = pgTable(\n  \"organization_membership\",\n  {\n    id: uuid(\"id\").defaultRandom().primaryKey(),\n    organizationId: uuid(\"organization_id\")\n      .notNull()\n      .references(() => organizations.id),\n    userId: uuid(\"user_id\")\n      .notNull()\n      .references(() => appUsers.id),\n    roleCode: membershipRole(\"role_code\").notNull(),\n    status: recordStatus(\"status\").default(\"ACTIVE\").notNull(),\n    createdAt: timestamp(\"created_at\", { withTimezone: true }).defaultNow().notNull(),\n    updatedAt: timestamp(\"updated_at\", { withTimezone: true }).defaultNow().notNull(),\n    version: bigint(\"version\", { mode: \"number\" }).default(1).notNull(),\n  },\n  (table) => [\n    uniqueIndex(\"organization_membership_org_user_uq\").on(\n      table.organizationId,\n      table.userId,\n    ),\n    index(\"organization_membership_user_idx\").on(table.userId),\n    index(\"organization_membership_org_idx\").on(table.organizationId),\n  ],\n);\n\nexport const auditEvents = pgTable(\n  \"audit_event\",\n  {\n    id: uuid(\"id\").defaultRandom().primaryKey(),\n    organizationId: uuid(\"organization_id\").references(() => organizations.id),\n    timestampUtc: timestamp(\"timestamp_utc\", { withTimezone: true })\n      .defaultNow()\n      .notNull(),\n    actorUserId: uuid(\"actor_user_id\").references(() => appUsers.id),\n    actorMembershipId: uuid(\"actor_membership_id\").references(\n      () => organizationMemberships.id,\n    ),\n    actorDisplayNameSnapshot: text(\"actor_display_name_snapshot\"),\n    ipAddress: text(\"ip_address\"),\n    moduleCode: text(\"module_code\").notNull(),\n    actionCode: text(\"action_code\").notNull(),\n    entityType: text(\"entity_type\").notNull(),\n    entityId: text(\"entity_id\").notNull(),\n    oldValueJson: jsonb(\"old_value_json\"),\n    newValueJson: jsonb(\"new_value_json\"),\n    requestId: text(\"request_id\"),\n    correlationId: text(\"correlation_id\"),\n    sourceChannel: text(\"source_channel\").notNull(),\n    reason: text(\"reason\"),\n  },\n  (table) => [\n    index(\"audit_event_org_time_idx\").on(table.organizationId, table.timestampUtc),\n    index(\"audit_event_entity_idx\").on(table.entityType, table.entityId),\n  ],\n);\n\nexport const outboxEvents = pgTable(\n  \"outbox_event\",\n  {\n    id: uuid(\"id\").defaultRandom().primaryKey(),\n    organizationId: uuid(\"organization_id\").references(() => organizations.id),\n    eventType: text(\"event_type\").notNull(),\n    aggregateType: text(\"aggregate_type\").notNull(),\n    aggregateId: text(\"aggregate_id\").notNull(),\n    payloadJson: jsonb(\"payload_json\").notNull(),\n    idempotencyKey: text(\"idempotency_key\").notNull(),\n    createdAt: timestamp(\"created_at\", { withTimezone: true }).defaultNow().notNull(),\n    availableAt: timestamp(\"available_at\", { withTimezone: true }).defaultNow().notNull(),\n    processedAt: timestamp(\"processed_at\", { withTimezone: true }),\n    attemptCount: bigint(\"attempt_count\", { mode: \"number\" }).default(0).notNull(),\n    lastError: text(\"last_error\"),\n  },\n  (table) => [\n    uniqueIndex(\"outbox_event_idempotency_uq\").on(table.idempotencyKey),\n    index(\"outbox_event_pending_idx\").on(table.processedAt, table.availableAt),\n  ],\n);\n");
files.set("src/lib/audit-diff.ts", "export type JsonRecord = Record<string, unknown>;\n\nexport type AuditDiff = {\n  oldValue: JsonRecord | null;\n  newValue: JsonRecord | null;\n};\n\nexport function auditDiff(\n  before: JsonRecord | null,\n  after: JsonRecord | null,\n): AuditDiff {\n  if (before === null || after === null) {\n    return { oldValue: before, newValue: after };\n  }\n\n  const keys = new Set([...Object.keys(before), ...Object.keys(after)]);\n  const oldValue: JsonRecord = {};\n  const newValue: JsonRecord = {};\n\n  for (const key of keys) {\n    const oldSerialized = JSON.stringify(before[key]);\n    const newSerialized = JSON.stringify(after[key]);\n\n    if (oldSerialized !== newSerialized) {\n      oldValue[key] = before[key] ?? null;\n      newValue[key] = after[key] ?? null;\n    }\n  }\n\n  return { oldValue, newValue };\n}\n");
files.set("src/lib/brand.ts", "export const brand = {\n  productName: process.env.NEXT_PUBLIC_PRODUCT_NAME?.trim() || \"Operations Platform\",\n  supportEmail: process.env.NEXT_PUBLIC_SUPPORT_EMAIL?.trim() || \"support@example.com\",\n  tagline: \"Clear work. Verified execution. Real-time visibility.\",\n} as const;\n");
files.set("src/lib/onboarding.ts", "export const onboardingStates = [\n  \"REGISTERED\",\n  \"PROFILE_COMPLETED\",\n  \"ORGANIZATION_CREATED\",\n  \"PLAN_SELECTED\",\n  \"BILLING_COMPLETE\",\n  \"FREE_OR_SPONSORED_ACTIVATED\",\n  \"FIRST_SITE_CREATED\",\n  \"ONBOARDING_COMPLETE\",\n] as const;\n\nexport type OnboardingState = (typeof onboardingStates)[number];\n\nconst allowedTransitions: Record<OnboardingState, readonly OnboardingState[]> = {\n  REGISTERED: [\"PROFILE_COMPLETED\"],\n  PROFILE_COMPLETED: [\"ORGANIZATION_CREATED\"],\n  ORGANIZATION_CREATED: [\"PLAN_SELECTED\"],\n  PLAN_SELECTED: [\"BILLING_COMPLETE\", \"FREE_OR_SPONSORED_ACTIVATED\"],\n  BILLING_COMPLETE: [\"FIRST_SITE_CREATED\"],\n  FREE_OR_SPONSORED_ACTIVATED: [\"FIRST_SITE_CREATED\"],\n  FIRST_SITE_CREATED: [\"ONBOARDING_COMPLETE\"],\n  ONBOARDING_COMPLETE: [],\n};\n\nexport function canAdvanceOnboarding(\n  from: OnboardingState,\n  to: OnboardingState,\n): boolean {\n  return allowedTransitions[from].includes(to);\n}\n");
files.set("src/lib/supabase/client.ts", "import { createBrowserClient } from \"@supabase/ssr\";\n\nexport function createClient() {\n  const url = process.env.NEXT_PUBLIC_SUPABASE_URL;\n  const key = process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;\n\n  if (!url || !key) {\n    throw new Error(\"Supabase browser environment variables are not configured.\");\n  }\n\n  return createBrowserClient(url, key);\n}\n");
files.set("src/lib/supabase/server.ts", "import { createServerClient } from \"@supabase/ssr\";\nimport { cookies } from \"next/headers\";\n\nexport async function createClient() {\n  const url = process.env.NEXT_PUBLIC_SUPABASE_URL;\n  const key = process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;\n\n  if (!url || !key) {\n    throw new Error(\"Supabase server environment variables are not configured.\");\n  }\n\n  const cookieStore = await cookies();\n\n  return createServerClient(url, key, {\n    cookies: {\n      getAll() {\n        return cookieStore.getAll();\n      },\n      setAll(cookiesToSet) {\n        try {\n          cookiesToSet.forEach(({ name, value, options }) =>\n            cookieStore.set(name, value, options),\n          );\n        } catch {\n          // Server Components cannot always write cookies.\n          // Session refresh will be handled by the request boundary once auth is enabled.\n        }\n      },\n    },\n  });\n}\n");
files.set("src/lib/tenant-context.ts", "import { z } from \"zod\";\n\nexport const tenantContextSchema = z.object({\n  userId: z.string().uuid(),\n  organizationId: z.string().uuid(),\n  membershipId: z.string().uuid(),\n});\n\nexport type TenantContext = z.infer<typeof tenantContextSchema>;\n\nexport function parseTenantContext(input: unknown): TenantContext {\n  return tenantContextSchema.parse(input);\n}\n");
files.set("tests/integration/README.md", "# Integration tests\n\nDatabase/RLS integration tests will be enabled immediately after the new vNext Supabase project is created.\n\nThey will use `TEST_DATABASE_URL` and will cover:\n- tenant context;\n- RLS positive/negative cases;\n- multi-Organization membership;\n- audit/value-change behavior;\n- claim/assignment concurrency;\n- migration and seed validation.\n\nNo integration test should point at the legacy database.\n");
files.set("tests/module/foundation-contract.test.ts", "import { describe, expect, it } from \"vitest\";\nimport { brand } from \"@/lib/brand\";\nimport { onboardingStates } from \"@/lib/onboarding\";\n\ndescribe(\"Foundation 0 product contract\", () => {\n  it(\"remains product-name configurable\", () => {\n    expect(brand.productName.length).toBeGreaterThan(0);\n  });\n\n  it(\"includes the required guided onboarding endpoints\", () => {\n    expect(onboardingStates).toContain(\"ORGANIZATION_CREATED\");\n    expect(onboardingStates).toContain(\"PLAN_SELECTED\");\n    expect(onboardingStates).toContain(\"FIRST_SITE_CREATED\");\n  });\n});\n");
files.set("tests/unit/audit-diff.test.ts", "import { describe, expect, it } from \"vitest\";\nimport { auditDiff } from \"@/lib/audit-diff\";\n\ndescribe(\"auditDiff\", () => {\n  it(\"captures only changed values\", () => {\n    expect(\n      auditDiff(\n        { assignedTo: \"Ravi\", status: \"PENDING\" },\n        { assignedTo: \"Priya\", status: \"PENDING\" },\n      ),\n    ).toEqual({\n      oldValue: { assignedTo: \"Ravi\" },\n      newValue: { assignedTo: \"Priya\" },\n    });\n  });\n\n  it(\"supports create and delete semantics\", () => {\n    expect(auditDiff(null, { name: \"Site A\" })).toEqual({\n      oldValue: null,\n      newValue: { name: \"Site A\" },\n    });\n  });\n});\n");
files.set("tests/unit/onboarding.test.ts", "import { describe, expect, it } from \"vitest\";\nimport { canAdvanceOnboarding } from \"@/lib/onboarding\";\n\ndescribe(\"onboarding state machine\", () => {\n  it(\"accepts the paid path\", () => {\n    expect(canAdvanceOnboarding(\"PLAN_SELECTED\", \"BILLING_COMPLETE\")).toBe(true);\n  });\n\n  it(\"accepts the free/sponsored path\", () => {\n    expect(canAdvanceOnboarding(\"PLAN_SELECTED\", \"FREE_OR_SPONSORED_ACTIVATED\")).toBe(true);\n  });\n\n  it(\"rejects skipping Organization creation\", () => {\n    expect(canAdvanceOnboarding(\"PROFILE_COMPLETED\", \"PLAN_SELECTED\")).toBe(false);\n  });\n});\n");
files.set("tests/unit/tenant-context.test.ts", "import { describe, expect, it } from \"vitest\";\nimport { parseTenantContext } from \"@/lib/tenant-context\";\n\ndescribe(\"tenant context\", () => {\n  it(\"requires user, organization, and membership UUIDs\", () => {\n    const parsed = parseTenantContext({\n      userId: \"11111111-1111-4111-8111-111111111111\",\n      organizationId: \"22222222-2222-4222-8222-222222222222\",\n      membershipId: \"33333333-3333-4333-8333-333333333333\",\n    });\n    expect(parsed.organizationId).toBe(\"22222222-2222-4222-8222-222222222222\");\n  });\n\n  it(\"rejects incomplete contexts\", () => {\n    expect(() => parseTenantContext({ userId: \"x\" })).toThrow();\n  });\n});\n");
files.set("tsconfig.json", "{\n  \"compilerOptions\": {\n    \"target\": \"ES2022\",\n    \"lib\": [\n      \"dom\",\n      \"dom.iterable\",\n      \"es2022\"\n    ],\n    \"allowJs\": false,\n    \"skipLibCheck\": true,\n    \"strict\": true,\n    \"noEmit\": true,\n    \"esModuleInterop\": true,\n    \"module\": \"esnext\",\n    \"moduleResolution\": \"bundler\",\n    \"resolveJsonModule\": true,\n    \"isolatedModules\": true,\n    \"jsx\": \"react-jsx\",\n    \"incremental\": true,\n    \"plugins\": [\n      {\n        \"name\": \"next\"\n      }\n    ],\n    \"paths\": {\n      \"@/*\": [\n        \"./src/*\"\n      ]\n    },\n    \"types\": [\n      \"node\"\n    ]\n  },\n  \"include\": [\n    \"next-env.d.ts\",\n    \"**/*.ts\",\n    \"**/*.tsx\",\n    \".next/types/**/*.ts\"\n  ],\n  \"exclude\": [\n    \"node_modules\"\n  ]\n}\n");
files.set("vitest.config.ts", "import { defineConfig } from \"vitest/config\";\n\nexport default defineConfig({\n  test: {\n    environment: \"node\",\n    include: [\"tests/**/*.test.ts\"],\n    coverage: {\n      reporter: [\"text\", \"json\", \"html\"],\n    },\n  },\n});\n");

for (const [rel, content] of files) {
  const file = path.join(root, rel);
  fs.mkdirSync(path.dirname(file), { recursive: true });
  fs.writeFileSync(file, content, "utf8");
}
console.log(`Foundation 0 wrote ${files.size} files.`);
console.log("Next: npm install");
