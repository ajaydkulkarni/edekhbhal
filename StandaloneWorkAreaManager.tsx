"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function StandaloneWorkAreaManager({properties}:{properties:{id:string;name:string}[]}){const router=useRouter();const[open,setOpen]=useState(false);const[error,setError]=useState("");const[saving,setSaving]=useState(false);async function create(fd:FormData){setSaving(true);setError("");const payload={propertyId:String(fd.get("propertyId")||""),name:String(fd.get("name")||""),description:String(fd.get("description")||""),locationIdentifier:String(fd.get("locationIdentifier")||"")};try{const r=await fetch("/api/work-areas",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to create Work Area");setOpen(false);router.refresh()}catch(e){setError(e instanceof Error?e.message:"Unable to create Work Area")}finally{setSaving(false)}}return <>{properties.length?<button className="button" onClick={()=>setOpen(true)}>Add Work Area</button>:<span className="muted">Create an active Property first</span>}{open&&<div className="modalBackdrop"><div className="modal"><h2>Create Work Area</h2><form action={create}><label>Parent Property<select name="propertyId" required>{properties.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>Work Area Name<input name="name" required minLength={2}/></label><label>Description<textarea name="description" rows={3}/></label><label>Exact Location Identifier<input name="locationIdentifier"/></label>{error&&<p className="error">{error}</p>}<div className="row"><button type="button" className="button secondary" onClick={()=>setOpen(false)}>Cancel</button><button className="button" disabled={saving}>{saving?"Creating...":"Create Work Area & QR"}</button></div></form></div></div>}</>}
