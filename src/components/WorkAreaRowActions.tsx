"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function WorkAreaRowActions({workArea}:{workArea:any}){const router=useRouter();const[editing,setEditing]=useState(false);const[error,setError]=useState("");async function patch(payload:any){setError("");const r=await fetch(`/api/work-areas/${workArea.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const d=await r.json();if(!r.ok)setError(d.error||"Unable to update Work Area");else{setEditing(false);router.refresh()}}return <><button className="button small secondary" onClick={()=>setEditing(true)}>View / Edit</button><button className="button small secondary" onClick={()=>patch({status:workArea.status==="ACTIVE"?"INACTIVE":"ACTIVE"})}>{workArea.status==="ACTIVE"?"Inactivate":"Reactivate"}</button>{editing&&<div className="modalBackdrop"><div className="modal"><h2>View / Edit Work Area</h2><p><strong>Parent Property:</strong> {workArea.property.name}</p><form action={fd=>patch({name:String(fd.get("name")||""),description:String(fd.get("description")||""),locationIdentifier:String(fd.get("locationIdentifier")||"")})}><label>Name<input name="name" defaultValue={workArea.name}/></label><label>Description<textarea name="description" defaultValue={workArea.description||""}/></label><label>Exact Location Identifier<input name="locationIdentifier" defaultValue={workArea.locationIdentifier||""}/></label>{error&&<p className="error">{error}</p>}<div className="row"><button type="button" className="button secondary" onClick={()=>setEditing(false)}>Cancel</button><button className="button">Save</button></div></form></div></div>}</>}
