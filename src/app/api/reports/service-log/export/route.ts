import * as XLSX from "xlsx";import {getSessionUser} from "@/lib/session";import {prisma} from "@/lib/prisma";import {zonedLocalToUtc} from "@/lib/schedule";
const dateOk=(v:string|null)=>v&&/^\d{4}-\d{2}-\d{2}$/.test(v)?v:"";function nextDate(v:string){const[y,m,d]=v.split("-").map(Number),x=new Date(Date.UTC(y,m-1,d));x.setUTCDate(x.getUTCDate()+1);return x.toISOString().slice(0,10)}const fd=(v:Date|null,tz:string)=>v?new Intl.DateTimeFormat("en-US",{timeZone:tz,year:"numeric",month:"2-digit",day:"2-digit"}).format(v):"";const ft=(v:Date|null,tz:string)=>v?new Intl.DateTimeFormat("en-US",{timeZone:tz,hour:"2-digit",minute:"2-digit",second:"2-digit",hour12:true}).format(v):"";function duration(s:number|null|undefined){if(s==null)return"";const n=Math.max(0,Math.floor(s));return`${String(Math.floor(n/3600)).padStart(2,"0")}:${String(Math.floor(n%3600/60)).padStart(2,"0")}:${String(n%60).padStart(2,"0")}`}const csvCell=(v:unknown)=>`"${String(v??"").replaceAll('"','""')}"`;
export async function GET(req:Request){const user=await getSessionUser();if(!user)return Response.json({error:"Unauthorized"},{status:401});const membership=await prisma.organizationMember.findFirst({where:{userId:user.id,status:"ACTIVE"},include:{organization:true}});if(!membership||!["ADMIN","PROPERTY_MANAGER"].includes(membership.role))return Response.json({error:"Forbidden"},{status:403});const u=new URL(req.url),format=u.searchParams.get("format")==="xlsx"?"xlsx":"csv",propertyId=u.searchParams.get("propertyId")||"",workAreaId=u.searchParams.get("workAreaId")||"",scheduleId=u.searchParams.get("scheduleId")||"",userId=u.searchParams.get("userId")||"",dateFrom=dateOk(u.searchParams.get("dateFrom")),dateTo=dateOk(u.searchParams.get("dateTo"));const dateFilter=dateFrom||dateTo?{...(dateFrom?{gte:zonedLocalToUtc(`${dateFrom}T00:00`,membership.organization.timezone)}:{}),...(dateTo?{lt:zonedLocalToUtc(`${nextDate(dateTo)}T00:00`,membership.organization.timezone)}:{})}:{not:null};const rows=await prisma.scheduleOccurrenceTask.findMany({where:{status:"COMPLETED",actualStartAt:dateFilter,occurrence:{organizationId:membership.organizationId,...(propertyId?{workArea:{propertyId}}:{}),...(workAreaId?{workAreaId}:{}),...(scheduleId?{scheduleId}:{}),...(userId?{assignedUserId:userId}:{})}},include:{occurrence:{include:{assignedUser:{select:{name:true,email:true}}}}},orderBy:{actualStartAt:"desc"},take:5000});const out=rows.map(r=>{const o=r.occurrence,a=r.actualDurationSeconds??(r.actualStartAt&&r.actualEndAt?Math.max(0,Math.floor((r.actualEndAt.getTime()-r.actualStartAt.getTime())/1000)):null),dev=a==null?null:a-r.plannedDurationMinutes*60;return{Property:o.propertyNameSnapshot,"Work Area":o.workAreaNameSnapshot,Schedule:o.scheduleNameSnapshot,"Document / SOP Reference":o.documentReferenceSnapshot??"","Revision / Version":o.documentRevisionSnapshot??"",Sequence:r.sequence,Task:r.taskNameSnapshot,"Actual Duration":duration(a),"Planned Duration":duration(r.plannedDurationMinutes*60),"Deviation Seconds":dev??"",User:o.assignedUser?.name??o.assignedUser?.email??"",Date:fd(r.actualStartAt,o.timezone),"Start Time":ft(r.actualStartAt,o.timezone),"End Time":ft(r.actualEndAt,o.timezone)}});const stamp=new Date().toISOString().slice(0,10);if(format==="xlsx"){const sheet=XLSX.utils.json_to_sheet(out),book=XLSX.utils.book_new();XLSX.utils.book_append_sheet(book,sheet,"Service Log");const binary=XLSX.write(book,{type:"buffer",bookType:"xlsx"});return new Response(new Uint8Array(binary),{headers:{"Content-Type":"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet","Content-Disposition":`attachment; filename="edekhbhal-service-log-${stamp}.xlsx"`,"Cache-Control":"private, no-store"}})}const fallback={Property:"","Work Area":"",Schedule:"","Document / SOP Reference":"","Revision / Version":"",Sequence:"",Task:"","Actual Duration":"","Planned Duration":"","Deviation Seconds":"",User:"",Date:"","Start Time":"","End Time":""},headers=Object.keys(out[0]??fallback),csv=[headers.map(csvCell).join(","),...out.map(row=>headers.map(h=>csvCell((row as any)[h])).join(","))].join("\r\n");return new Response("\uFEFF"+csv,{headers:{"Content-Type":"text/csv; charset=utf-8","Content-Disposition":`attachment; filename="edekhbhal-service-log-${stamp}.csv"`,"Cache-Control":"private, no-store"}})}
