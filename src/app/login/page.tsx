"use client";
import { useState } from "react";
import Link from "next/link";
export default function Login(){const [email,setEmail]=useState("");const [message,setMessage]=useState("");const [error,setError]=useState("");return <main className="container"><div className="card" style={{maxWidth:520,margin:"60px auto"}}><h1>Login</h1><p className="muted">Enter your email to receive a secure authentication link.</p><form onSubmit={async e=>{e.preventDefault();setError("");const r=await fetch("/api/auth/request-link",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const j=await r.json();if(!r.ok)setError(j.error);else setMessage(j.message+(j.devLink?` Development link: ${j.devLink}`:""));}}><label className="label">Email</label><input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/><div style={{marginTop:18}}><button className="button">Send authentication link</button></div></form>{error&&<p className="error">{error}</p>}{message&&<p className="success">{message}</p>}<p className="muted">New to eDekhbhal? <Link href="/register">Register</Link></p></div></main>}
