import Link from "next/link";import {redirect} from "next/navigation";import {Nav} from "@/components/Nav";import {getSessionUser} from "@/lib/session";import {prisma} from "@/lib/prisma";import {zonedLocalToUtc} from "@/lib/schedule";
type Q=string|string[]|undefined;type SP=Record<string,Q>;const first=(v:Q)=>Array.isArray(v)?v[0]??"":v??"";const vd=(v:string)=>/^\d{4}-\d{2}-\d{2}$/.test(v)?v:"";function nextDate(v:string){const[y,m,d]=v.split("-").map(Number),x=new Date(Date.UTC(y,m-1,d));x.setUTCDate(x.getUTCDate()+1);return x.toISOString().slice(0,10)}function localDate(back:number,tz:string){const p=Object.fromEntries(new Intl.DateTimeFormat("en-CA",{timeZone:tz,year:"numeric",month:"2-digit",day:"2-digit"}).formatToParts(new Date()).map(x=>[x.type,x.value])),d=new Date(Date.UTC(+p.year,+p.month-1,+p.day));d.setUTCDate(d.getUTCDate()-back);return d.toISOString().slice(0,10)}const pct=(c:number,t:number)=>t?`${(c/t*100).toFixed(1)}%`:"—";const fmt=(v:Date|null,tz:string)=>v?new Intl.DateTimeFormat("en-US",{timeZone:tz,dateStyle:"short",timeStyle:"short"}).format(v):"—";
export default async function Page({searchParams}:{searchParams:Promise<SP>}){const user=await getSessionUser();if(!user)redirect("/login");const m=await prisma.organizationMember.findFirst({where:{userId:user.id,status:"ACTIVE"},include:{organization:true}});if(!m)redirect("/onboarding");if(!["ADMIN","PROPERTY_MANAGER"].includes(m.role))redirect("/dashboard");const q=await searchParams,propertyId=first(q.propertyId),workAreaId=first(q.workAreaId),scheduleId=first(q.scheduleId),userId=first(q.userId),status=first(q.status),dateFrom=vd(first(q.dateFrom))||localDate(6,m.organization.timezone),dateTo=vd(first(q.dateTo))||localDate(0,m.organization.timezone),start=zonedLocalToUtc(`${dateFrom}T00:00`,m.organization.timezone),end=zonedLocalToUtc(`${nextDate(dateTo)}T00:00`,m.organization.timezone);
const[properties,workAreas,schedules,members,occ]=await Promise.all([prisma.property.findMany({where:{organizationId:m.organizationId},select:{id:true,name:true},orderBy:{name:"asc"}}),prisma.workArea.findMany({where:{property:{organizationId:m.organizationId},...(propertyId?{propertyId}:{})},select:{id:true,name:true,property:{select:{name:true}}},orderBy:{name:"asc"}}),prisma.schedule.findMany({where:{organizationId:m.organizationId,...(workAreaId?{workAreaId}:{})},select:{id:true,name:true},orderBy:{name:"asc"}}),prisma.organizationMember.findMany({where:{organizationId:m.organizationId},select:{user:{select:{id:true,name:true,email:true}}}}),prisma.scheduleOccurrence.findMany({where:{organizationId:m.organizationId,scheduledStartAt:{gte:start,lt:end},status:{in:["COMPLETED","PARTIALLY_COMPLETED","MISSED"],...(status?{equals:status as any}:{})},...(workAreaId?{workAreaId}:{}),...(scheduleId?{scheduleId}:{}),...(userId?{assignedUserId:userId}:{}),...(propertyId?{workArea:{propertyId}}:{})},include:{assignedUser:{select:{id:true,name:true,email:true}}},orderBy:{scheduledStartAt:"desc"},take:1000})]);
const completed=occ.filter(o=>o.status==="COMPLETED").length,partial=occ.filter(o=>o.status==="PARTIALLY_COMPLETED").length,missed=occ.filter(o=>o.status==="MISSED").length;
type Bucket={label:string;completed:number;partial:number;missed:number;total:number};
function bucketBy(selector:(row:(typeof occ)[number])=>string):Bucket[]{const map=new Map<string,Bucket>();for(const row of occ){const label=selector(row)||"Unassigned",item=map.get(label)??{label,completed:0,partial:0,missed:0,total:0};item.total++;if(row.status==="COMPLETED")item.completed++;if(row.status==="PARTIALLY_COMPLETED")item.partial++;if(row.status==="MISSED")item.missed++;map.set(label,item)}return[...map.values()].sort((a,b)=>b.missed-a.missed||b.total-a.total||a.label.localeCompare(b.label))}
const scheduleSummary=bucketBy(o=>o.scheduleNameSnapshot),propertySummary=bucketBy(o=>o.propertyNameSnapshot),workAreaSummary=bucketBy(o=>o.workAreaNameSnapshot),userSummary=bucketBy(o=>o.assignedUser?.name??o.assignedUser?.email??"Unassigned");return <><Nav/><main className="container" style={{maxWidth:1600}}><div className="breadcrumbs"><Link href="/reports">Reports</Link> / Service Compliance</div><h1>Service Compliance & Analytics</h1><p className="muted">Controlled-document reference and revision are occurrence snapshots, so audit history remains tied to the checklist version actually used.</p>
<form method="get" className="card"><div className="formGrid"><label>From<input type="date" name="dateFrom" defaultValue={dateFrom}/></label><label>To<input type="date" name="dateTo" defaultValue={dateTo}/></label><label>Property<select name="propertyId" defaultValue={propertyId}><option value="">All Properties</option>{properties.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>Work Area<select name="workAreaId" defaultValue={workAreaId}><option value="">All Work Areas</option>{workAreas.map(w=><option key={w.id} value={w.id}>{w.name} — {w.property.name}</option>)}</select></label><label>Schedule<select name="scheduleId" defaultValue={scheduleId}><option value="">All Schedules</option>{schedules.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label>User<select name="userId" defaultValue={userId}><option value="">All Users</option>{members.sort((a,b)=>(a.user.name??a.user.email).localeCompare(b.user.name??b.user.email)).map(x=><option key={x.user.id} value={x.user.id}>{x.user.name??x.user.email}</option>)}</select></label><label>Status<select name="status" defaultValue={status}><option value="">All final statuses</option><option value="COMPLETED">Completed</option><option value="PARTIALLY_COMPLETED">Partially Completed</option><option value="MISSED">Missed</option></select></label></div><div className="row" style={{marginTop:12}}><button className="button">Apply Filters</button><Link className="button secondary" href="/reports/compliance">Reset</Link></div></form>
<div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(170px,1fr))",gap:12,margin:"18px 0"}}><K label="Occurrences" v={occ.length}/><K label="Completed" v={completed}/><K label="Partial" v={partial}/><K label="Missed" v={missed}/><K label="Service compliance" v={pct(completed+partial,occ.length)}/></div>
<Summary title="By Schedule" rows={scheduleSummary}/><Summary title="By Property" rows={propertySummary}/><Summary title="By Work Area" rows={workAreaSummary}/><Summary title="By User" rows={userSummary}/><div className="card" style={{marginTop:18,overflowX:"auto"}}><h2>Occurrence Detail</h2><table className="table"><thead><tr><th>Scheduled</th><th>Property</th><th>Work Area</th><th>Schedule</th><th>Document Ref.</th><th>Revision</th><th>Status</th><th>User</th><th>Reason</th><th></th></tr></thead><tbody>{occ.map(o=><tr key={o.id}><td>{fmt(o.scheduledStartAt,o.timezone)}</td><td>{o.propertyNameSnapshot}</td><td>{o.workAreaNameSnapshot}</td><td>{o.scheduleNameSnapshot}</td><td>{o.documentReferenceSnapshot??"—"}</td><td>{o.documentRevisionSnapshot??"—"}</td><td>{o.status.replaceAll("_"," ")}</td><td>{o.assignedUser?.name??o.assignedUser?.email??"—"}</td><td>{o.missedReason??"—"}</td><td><Link href={`/reports/occurrences/${o.id}`}>Open</Link></td></tr>)}{!occ.length?<tr><td colSpan={10} className="muted">No finalized occurrences match these filters.</td></tr>:null}</tbody></table></div></main></>}
function K({label,v}:{label:string;v:string|number}){return <div className="card"><div className="muted">{label}</div><div style={{fontSize:28,fontWeight:800,marginTop:6}}>{v}</div></div>}

function Summary({title,rows}:{title:string;rows:Array<{label:string;completed:number;partial:number;missed:number;total:number}>}){return <div className="card" style={{marginTop:18,overflowX:"auto"}}><h2 style={{marginTop:0}}>{title}</h2><table className="table"><thead><tr><th>{title.replace("By ","")}</th><th>Total</th><th>Completed</th><th>Partial</th><th>Missed</th><th>Compliance</th></tr></thead><tbody>{rows.slice(0,50).map(r=><tr key={r.label}><td>{r.label}</td><td>{r.total}</td><td>{r.completed}</td><td>{r.partial}</td><td>{r.missed}</td><td>{pct(r.completed+r.partial,r.total)}</td></tr>)}{!rows.length?<tr><td colSpan={6} className="muted">No data.</td></tr>:null}</tbody></table></div>}
