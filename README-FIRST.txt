Batch 03 pre-commit TypeScript correction.

Upload the extracted `batch-payload` folder to GitHub, preserving the path.
This replaces only:
batch-payload/src/app/api/e2e/reported-work/route.ts

Then in Codespaces:
git pull
bash APPLY-finetune-batch-03.sh
bash CHECK-finetune-batch-03.sh

Do not commit the currently applied Batch 03 changes until CHECK completes successfully.
